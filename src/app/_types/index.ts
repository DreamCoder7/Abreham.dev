export interface IntroProps {
  title: string;
  text: string;
  btnText: string;
}

export interface LottieAnimationProps {
  animationData: any;
  loop?: boolean;
  autoplay?: boolean;
  speed?: number;
}
